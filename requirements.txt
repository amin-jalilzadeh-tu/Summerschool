# ---------------------------------------------------------------------------
# Case Study 2 - Python dependencies
#
# Python 3.9 or newer.  Validated in a clean virtual environment on Python 3.14.6.
#
# Install with:
#     pip install -r requirements.txt
#
# If the coloured table cell in Q1 fails with "ImportError: ... jinja2",
# it means jinja2 is missing - it is listed below and pip installs it.
# ---------------------------------------------------------------------------

# --- analysis libraries (what the notebook code uses) ---
numpy>=1.23          # arithmetic, random weights (SMAA)
pandas>=1.5          # CSV loading, tables, Styler (needs .apply_index -> pandas >= 1.4)
jinja2>=3.0          # REQUIRED by pandas Styler to render the green/red table
scipy>=1.9           # used in later notebooks (linear programming)
matplotlib>=3.5      # used in later notebooks (plots)

# --- to open and run the notebooks ---
jupyterlab>=4.0      # the notebook interface
ipykernel>=6.0       # lets the environment run as a notebook kernel

# --- exact versions the clean 'summerschool' venv installed and ran on ---
# (Python 3.14.6; pin these instead of the ranges above for an identical setup)
#   numpy==2.5.1
#   pandas==3.0.3
#   jinja2==3.1.6
#   scipy==1.18.0
#   matplotlib==3.11.1
#   jupyterlab==4.6.2
#   ipykernel==7.3.0
#   nbconvert==7.17.1   (pulled in by jupyterlab)
